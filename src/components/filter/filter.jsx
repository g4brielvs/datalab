import React from 'react';
import filterStyles from './filter.module.scss';

const Filter = ({props}) => (<span className={'filter-button ' + filterStyles.filterButton}>Filter</span>);


export default Filter
